# 生产环境配置示例
# 复制此文件为 .env 并修改值

# Node环境
NODE_ENV=production

# 服务端口 (Docker内部使用，通常无需修改)
PORT=3000

# JWT 安全密钥 (!!! 必须修改为随机字符串 !!!)
# 生成方式: openssl rand -hex 32
JWT_SECRET=replace_this_with_a_very_long_random_string_at_least_32_chars

# Token 过期时间
JWT_EXPIRES_IN=7d
